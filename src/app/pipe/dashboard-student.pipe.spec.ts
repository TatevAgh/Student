import { DashboardStudentPipe } from './dashboard-student.pipe';

describe('DashboardStudentPipe', () => {
  it('create an instance', () => {
    const pipe = new DashboardStudentPipe();
    expect(pipe).toBeTruthy();
  });
});
